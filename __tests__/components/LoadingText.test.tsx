import { render } from "@testing-library/react";
import LoadingText from "../../components/LoadingText";


describe('Testing components/LoadingText', () => {
    it("", () => {
        render(<LoadingText currentStage={""} isProof={false} />)
    });
});